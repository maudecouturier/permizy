<div class="navbar navbar-expand-sm navbar-light navbar-cardetour">
  <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to new_booking_path, class: "btn btn-warning btn-cardetour", data: { turbolinks: false }  do %>
          <i class="fas fa-plus mr-2"></i>RESERVER UN COURS
          <% end %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Déconnexion", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
